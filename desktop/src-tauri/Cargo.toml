[package]
name = "droidtrans"
version = "1.0.0"
description = "DroidTrans / 卓传 desktop"
authors = ["MK"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = true
